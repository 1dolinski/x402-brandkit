# Use mock API for development (set to false in production if x402 API is available)
NEXT_PUBLIC_USE_MOCK=true

# x402 API base URL (customize if needed)
# NEXT_PUBLIC_X402_API_BASE=https://x402.com/api
